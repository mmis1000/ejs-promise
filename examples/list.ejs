<% if (names.length) { %>
  <ul>
    <% for (var i; i < names.length; i++) { name = names[i]; %>
      <%# Notice how the single quotation mark is escaped in the output HTML %>
      <li foo='<%= name + "'" %>'><%= name %></li>
    <% } %>
  </ul>
<% } %>
